#!/usr/bin/env ruby
require "bundler/setup"
require "femis_hangman"

s = FemisHangman::Router.new

if ARGV.size == 1
  s.status = 'show'
  s.load_game(ARGV[0].to_i)
  s.loop
else
  s.invalid_prompt
  s.help_prompt
  s.show_saved_games
end