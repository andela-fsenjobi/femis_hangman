#!/usr/bin/env ruby
require "bundler/setup"
require "femis_hangman"

s = FemisHangman::Router.new

if ARGV.length == 2
  s.status = 'show'
  s.load_game(ARGV[1])
  s.loop
else
  session.invalid_prompt
end