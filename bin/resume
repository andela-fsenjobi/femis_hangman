#!/usr/bin/env ruby
require "bundler/setup"
require "femis_hangman"

s = FemisHangman::Router.new


if ARGV.length > 2
  session.invalid_prompt
else
  s.status = 'show'
  s.show_saved_games
  s.loop
end