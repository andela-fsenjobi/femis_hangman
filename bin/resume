#!/usr/bin/env ruby
require "bundler/setup"
require "femis_hangman"

s = FemisHangman::Router.new

if ARGV.length == 2
  s.status = 'show'
  s.load_game(ARGV[1])
  s.loop
else
  s.invalid_prompt
  s.help_prompt
  s.load_prompt
end